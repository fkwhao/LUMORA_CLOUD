spring:
  application:
    name: lumora-model-catalog-service
  cloud:
    nacos:
      discovery:
        server-addr: ${LUMORA_NACOS_ADDR:127.0.0.1:8848}

server:
  port: ${LUMORA_CATALOG_PORT:46103}

management:
  endpoints:
    web:
      exposure:
        include: health,info
